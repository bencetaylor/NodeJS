<% include headeruser %>
<div class="col-md-push-3 col-md-6 col-xs-12">
    <h2>Car list</h2>
    <table class="table table-stripped">
        <thead>
        <tr>
            <th>Brand</th>
            <th>Type</th>
            <th>Number Plate</th>
            <th>Price</th>
            <th>&nbsp;</th>
        </tr>
        </thead>
        <tbody>

        <% cars.forEach(function(item){ %>
        <tr>
            <td><% item.brand %></td>
            <td><% item.type %></td>
            <td><% item.plate%></td>
            <td><% item.price %> €/day</td>
            <td class="text-right">
                <a href="/cars/<%= item.id %>/edit" class="btn btn-sm btn-primary">Edit</a>
                <a href="#" class="btn btn-sm btn-danger">Delete</a>
            </td>
        </tr>
        <% }); %>

        </tbody>
    </table>
    <div class="text-center">
        <a href="/cars/add" class="btn btn-lg btn-primary">New car</a>
    </div>
</div>
<% include footer %>